# AGENTS.md

## 本仓库约定

- 金额单位一律用分，接口层展示时才换算成元。
- 改订单 status 必须走 `advanceOrder`，不允许直接赋值。

## 检索约定

查业务规则、流程或约束时，按以下顺序，禁止跳步：

1. 先读 llms.txt，用各条目的说明定位候选文档
2. 完整读回候选文档；能回答就到此为止
3. 索引里没有相关条目时，才允许 grep 全仓库兜底
4. 兜底得到的结论，标注"来自代码反推，未经文档确认"
